FROM python:3.11-slim

WORKDIR /app

COPY admin/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY admin/ ./admin/
COPY config/ ./config/

CMD ["uvicorn", "admin.admin_api:app", "--host", "0.0.0.0", "--port", "8787"]
